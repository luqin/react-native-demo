import React from 'react';

import {
  Text,
  View,
} from 'react-native';

class Button extends React.Component {

  static propTypes = {};

  static defaultProps = {};

  constructor(props) {
    super(props);
    this.state = {};
  }

  componentDidMount() {
  }

  render() {
    return (
      <div>Button</div>
    );
  }
}

export default Button;
